import { CurrenyFormatDirective } from './curreny-format.directive';

describe('CurrenyFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrenyFormatDirective();
    expect(directive).toBeTruthy();
  });
});
